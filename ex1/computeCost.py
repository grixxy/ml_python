import numpy as np

def computeCost(X, y, theta):
    m = np.size(y) # number of training examples


    #X_THETA = X.dot(theta)
    #J = 1./(2.*m)*np.sum((X_THETA-y)**2)
    #print(J)
    return J[0][0]